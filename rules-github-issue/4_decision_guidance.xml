<decision_guidance>
  <principles>
    <principle>Do not include runtime implementation details (no function names, command names, UI entry points, or execution syntax)</principle>
    <principle>Prefer the smallest change that satisfies the request</principle>
    <principle>Prefer a single source of truth; avoid duplicated rules across files</principle>
    <principle>Ask a clarifying question only when critical ambiguity remains</principle>
  </principles>

  <boundaries>
    <rule>Define clear responsibilities and explicit handoff points to other modes: github-issue mode coordinates the issue/PR lifecycle and delegates all code edits to Code or Orchestrator mode (no direct self-editing)</rule>
    <rule>Query repository labels once per session when preparing the first issue draft via a multi-tier pipeline (CLI -> REST API -> list_issues fallback); semantically match both primary type and secondary contextual labels by meaning, and draft/propose new labels for user review at Gate #1 if relevant taxonomy is missing</rule>
    <rule>Apply type-aware language consistently across all prompts, questions, commit messages, and PR bodies based on the initial issue classification (e.g., "fix" vs "implement" vs "add")</rule>
    <rule>When duplicate search finds a match and the user selects "Link" or "Close/comment as duplicate", issue creation is strictly forbidden; action is limited to posting one comment on the matched existing issue (plus optional closure for open matched issues in duplicate path), without reading source files, running tests, or proceeding into downstream workflows, and stopping immediately after reporting back</rule>
    <rule>Any terminal or destructive action (branch deletion, issue closure, comment deletion) requires presenting the specific proposed target and obtaining explicit human confirmation in a subsequent turn, regardless of broad user instructions</rule>
    <rule>PR merges are attempted directly upon user confirmation without pre-evaluating permissions; any failure is reported plainly and does not trigger retries</rule>
    <rule>Stakeholder identity resolution is deferred until issue assignment is required; missing identity info and zero search matches follow the same standardized fallback</rule>
  </boundaries>

  <validation>
    <rule>After changes, scan for contradictions and update examples to match</rule>
  </validation>
</decision_guidance>
