<error_handling>
  <error_scenarios>
    <scenario>
      <situation>Remote origin URL is missing or invalid</situation>
      <solution>Prompt user to configure git remote or manually supply owner/repo name.</solution>
    </scenario>
    <scenario>
      <situation>GitHub MCP API authentication or permission error</situation>
      <solution>Verify GitHub authentication settings or personal access token permissions.</solution>
    </scenario>
    <scenario>
      <situation>Duplicate issue found</situation>
      <solution>Present the existing issue URL to the user and ask whether to link, comment, or override.</solution>
    </scenario>
    <scenario>
      <situation>Accidental acceptance criteria leakage attempted</situation>
      <solution>Intercept draft creation, strip acceptance criteria from public markdown payload, and retain them solely in private scratchpad.</solution>
    </scenario>
    <scenario>
      <situation>Fix verification fails</situation>
      <solution>Report test failures or unmet acceptance criteria back to the user before attempting closure.</solution>
    </scenario>
    <scenario>
      <situation>Attempted direct API issue closure (`github.update_issue` with state closed)</situation>
      <solution>Block execution immediately. Reiterate that issues must ONLY be closed automatically via GitHub native PR merge webhooks using plain-text keywords in PR description.</solution>
    </scenario>
    <scenario>
      <situation>Attempted bypass of Gate #1 draft review or Gate #3 pre-push review</situation>
      <solution>Halt execution immediately, present draft or git diff / test output, and wait for explicit human confirmation.</solution>
    </scenario>
    <scenario>
      <situation>Sub-mode (Code/Fix) attempts unauthorized git commit, push, or PR creation</situation>
      <solution>Reject action, return control to github-issue mode, and enforce Gate #3 human review.</solution>
    </scenario>
    <scenario>
      <situation>Attempted premature task completion before Phase 6 workspace cleanup</situation>
      <solution>Block completion, execute CAN_MERGE check, offer/execute PR merge, restore workspace via `git checkout main &amp;&amp; git pull`, and delete remote feature branch.</solution>
    </scenario>
  </error_scenarios>
</error_handling>
