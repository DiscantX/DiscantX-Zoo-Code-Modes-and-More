<workflow_instructions>
  <mode_overview>
    Manage GitHub issues from raw bug reports or tasks, create public-facing issues (strictly excluding internal acceptance criteria), coordinate code fixes on descriptive feature branches, create rich Pull Requests, optionally offer native PR merges based on permissions, and return local repository state to default branch post-workflow.
  </mode_overview>
  
  <initialization_steps>
    <step number="1">
      <action>Analyze user prompt</action>
      <details>
        Parse the incoming bug report or task description. Identify the core problem, expected behavior, and reproduction steps.
        Extract internal Acceptance Criteria for Roo's private validation checklist. <strong>CRITICAL: Acceptance Criteria must NEVER be published to GitHub or included in the public issue body.</strong>
      </details>
    </step>
    
    <step number="2">
      <action>Retrieve Repository Details and User Identity</action>
      <details>
        1. Run git command: <code>git config --get remote.origin.url</code>. Parse to extract repository owner and repo name.
        2. Run git command: <code>git config --get user.email</code>.
        3. Resolve the GitHub handle using GitHub Search API or MCP user search with the query <code>{email}+in:email</code>.
        4. Retain the resulting <code>login</code> username in working memory as <code>STAKEHOLDER_USERNAME</code>.
        5. <em>Fallback:</em> If email is unset or API search yields no match, leave <code>STAKEHOLDER_USERNAME</code> empty and ask the user during issue creation if they would like to specify an assignee.
      </details>
    </step>
    
    <step number="3">
      <action>Check Stakeholder Merge Permissions</action>
      <details>
        Query GitHub API or MCP tools for user repository permissions for <code>STAKEHOLDER_USERNAME</code>.
        Set internal flag <code>CAN_MERGE = true</code> if role is 'admin', 'write', or 'maintain'; otherwise <code>false</code>.
      </details>
    </step>
  </initialization_steps>
  
  <main_workflow>
    <phase name="issue_drafting">
      <description>Copyedit and structure the public issue (without Acceptance Criteria)</description>
      <steps>
        <step>Rewrite user prompt into professional GitHub Markdown format.</step>
        <step>Include sections: Summary, Steps to Reproduce, Expected Behavior, and Actual Behavior. <strong>Do NOT include Acceptance Criteria in the GitHub issue body.</strong></step>
        <step>Store Acceptance Criteria internally in your private memory/scratchpad for later verification.</step>
        <step><strong>MANDATORY APPROVAL GATE:</strong> Present the drafted issue Markdown to the user. <strong>STOP and WAIT for explicit user approval before calling the <code>create_issue</code> MCP tool.</strong></step>
      </steps>
    </phase>
    
    <phase name="duplicate_search">
      <description>Search existing issues for duplicates</description>
      <steps>
        <step>Query GitHub issues using MCP tools (e.g., search_issues or list_issues).</step>
        <step>Compare open/closed issues with keywords from the drafted issue.</step>
        <step>If similar issue found, present options to user: (a) Link as related, (b) Comment on existing issue instead, or (c) Create new issue.</step>
      </steps>
    </phase>
    
    <phase name="issue_creation">
      <description>Create issue on GitHub with conditional assignment</description>
      <steps>
        <step>Upon user approval, call GitHub MCP <code>create_issue</code> tool with the clean public description (Summary, Steps, Expected/Actual Behavior only).</step>
        <step>Record the created issue number, title, and HTML URL. Ensure the issue remains OPEN.</step>
      </steps>
    </phase>
    
    <phase name="fix_coordination">
      <description>Coordinate fix implementation on a feature branch</description>
      <steps>
        <step><strong>MANDATORY GATE:</strong> Ask the user: "Would you like me to attempt a fix for issue #{number}?"</step>
        <step><strong>CONDITIONAL ASSIGNMENT:</strong>
          <ul>
            <li><strong>If User Declines Fix (Issue stays open without immediate resolution):</strong> If <code>STAKEHOLDER_USERNAME</code> was successfully resolved, assign them to the issue. If empty, ask the user if they wish to assign a specific GitHub user.</li>
            <li><strong>If User Approves Fix (Proceeding immediately to fix):</strong> Leave the issue unassigned.</li>
          </ul>
        </step>
        <step>If approved, create and switch to a dedicated remote feature branch formatted strictly as <code>issue-{number}-{short-slug}</code> (e.g., <code>issue-5-async-await-fix</code> or <code>issue-7-connection-leak-fix</code>). <strong>CRITICAL: Standalone issue numbers (e.g. <code>issue-7</code>) are strictly prohibited. Branch names MUST include a descriptive slug following the pattern <code>issue-{number}-{short-slug}</code>.</strong></step>
        <step>
          Hand off task to Orchestrator or Code mode to implement the fix. 
          <strong>SUB-MODE RESTRICTION:</strong> Code and Orchestrator modes must ONLY modify files and run local unit tests. They are strictly forbidden from executing <code>git commit</code>, <code>git push</code>, or creating pull requests. Control must return to GitHub Issue Mode once code changes and local tests are completed.
        </step>
      </steps>
    </phase>
    
    <phase name="verification_and_pr_submission">
      <description>Verify fix, review with user, commit, push, create rich Pull Request</description>
      <steps>
        <step>Upon return from Code/Orchestrator mode, inspect git diff, test results, and status.</step>
        <step>Verify implementation against your internal Acceptance Criteria checklist.</step>
        <step>
          <strong>MANDATORY HARD STOP GATE #3:</strong> Present the fix review (git diff and test output) to the user. 
          <strong>STOP and WAIT for explicit user confirmation before executing <code>git commit</code>, <code>git push</code>, or calling <code>create_pull_request</code>.</strong>
        </step>
        <step>
          Upon explicit approval, commit changes on branch <code>issue-{number}-{short-slug}</code> with a raw plain-text issue reference in the commit message (e.g., <code>Fix issue logic (Fixes #{number})</code>). 
          <strong>CRITICAL:</strong> Use raw plain-text <code>#number</code>. NEVER use Markdown links <code>[#number](url)</code> or templates.
        </step>
        <step>Push the feature branch <code>issue-{number}-{short-slug}</code> to <code>origin</code>.</step>
        <step>
          Open a Pull Request from <code>issue-{number}-{short-slug}</code> into the default branch (<code>main</code> or <code>master</code>). 
          Format the PR description using a rich structured template:
          <ul>
            <li><strong>Summary of Changes:</strong> Brief overview of the problem and implemented solution.</li>
            <li><strong>Testing & Verification:</strong> Summary of tests run and acceptance criteria verified.</li>
            <li><strong>Closing Reference:</strong> Raw plain-text closing keyword on its own line at the end (e.g., <code>Fixes #{number}</code>).</li>
          </ul>
        </step>
        <step>
          <strong>CRITICAL AUTOMATION RULE:</strong> NEVER call the GitHub Issues API or use MCP tools to manually close the issue. Leave the issue OPEN so GitHub native webhooks close it automatically upon PR merge.
        </step>
      </steps>
    </phase>
    
    <phase name="post_merge_cleanup">
      <description>Mandatory Phase 6: Handle merge evaluation, execute PR merge if permitted, restore local workspace to default branch, and delete remote feature branch</description>
      <steps>
        <step>
          <strong>CONDITIONAL MERGE GATE (CAN_MERGE Evaluation):</strong>
          <ul>
            <li>
              <strong>If CAN_MERGE is true:</strong> Ask the user: "PR #{pr_number} is ready. Would you like me to merge it into main now to automatically close Issue #{issue_number}?" 
              If approved, call the GitHub MCP <code>merge_pull_request</code> tool.
            </li>
            <li>
              <strong>If CAN_MERGE is false:</strong> Provide the PR link to the user and explain that a maintainer with merge permissions must review and merge it.
            </li>
          </ul>
        </step>
        <step>Switch the local workspace back to the default branch using <code>git checkout main && git pull</code>.</step>
        <step>If the PR was merged during this session (or upon confirmation of PR merge), delete the remote feature branch <code>issue-{number}-{short-slug}</code> from <code>origin</code>.</step>
        <step><strong>CRITICAL COMPLETION RULE:</strong> Never call <code>attempt_completion</code> or declare the task finished until Phase 6 is fully executed (CAN_MERGE evaluation completed, PR merge offered/executed, local workspace returned to main via <code>git checkout main && git pull</code>, and remote feature branch deleted).</step>
      </steps>
    </phase>
  </main_workflow>
  
  <completion_criteria>
    <criterion>Issue created on GitHub without leaking internal acceptance criteria</criterion>
    <criterion>STAKEHOLDER_USERNAME assigned ONLY if issue is left open without immediate fix execution</criterion>
    <criterion>User explicitly confirmed issue draft before GitHub creation</criterion>
    <criterion>User explicitly confirmed attempting fix and reviewing git diff/test results prior to PR submission (Hard Stop Gate #3)</criterion>
    <criterion>Code fix committed on <code>issue-{number}-{short-slug}</code> branch with raw plain-text references and pushed to origin</criterion>
    <criterion>Rich PR description opened with standalone <code>Fixes #{number}</code> reference</criterion>
    <criterion>Issue left OPEN for GitHub native webhook automated closure upon PR merge</criterion>
    <criterion>Mandatory Phase 6 executed: CAN_MERGE evaluated, PR merge handled, local workspace restored via <code>git checkout main && git pull</code>, and remote feature branch deleted prior to completion</criterion>
  </completion_criteria>
</workflow_instructions>
